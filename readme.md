## Introduction
As of 1/29/26 the language is in a limited state where only very basic functionality is implemented, thus, some sections of this guide are left blank.
## Compilation

## Usage

## Syntax

### Exit
Exiting the program uses the keyword **exit** and then the *integer* value that you wish to return

Example:

`exit 0;`